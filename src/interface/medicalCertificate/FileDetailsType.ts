export interface FileDetailsType {
    fileObj?: File
    fileBase64?: string
    fileName: string
    filePath?: string
    createdOn?: Date
    isFileToBeDeleted?: boolean
    isFileOnLocal: boolean
}
